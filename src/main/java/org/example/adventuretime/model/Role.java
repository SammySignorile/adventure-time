package org.example.adventuretime.model;

/**
 * Names match the existing MySQL enum. In the UI CLIENTE is presented as
 * "Viaggiatore" and GESTORE as "Venditore".
 */
public enum Role {
    CLIENTE,
    GESTORE,
    ADMIN
}
